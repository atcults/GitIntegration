{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceRoot}/src/GitIntegration/GitIntegration.csproj"
            ],
            "problemMatcher": "$msCompile",
            "group": "build"
        }
    ]
}
